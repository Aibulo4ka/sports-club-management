# Спринт 5 - Платежи: Инструкция по запуску и тестированию

## 📋 Что реализовано в Спринте 5

### ✅ Готовые компоненты

1. **Backend API**
   - `PaymentSerializer` - сериализаторы для платежей
   - `PaymentViewSet` - API endpoints для управления платежами
   - `PaymentCreateSerializer` - создание платежей с автоматическим расчетом скидок
   - Endpoints:
     - `GET /api/payments/` - список всех платежей (админ)
     - `GET /api/payments/my/` - мои платежи (клиент)
     - `GET /api/payments/{id}/` - детали платежа
     - `POST /api/payments/` - создание платежа
     - `GET /api/payments/{id}/status_check/` - проверка статуса
     - `POST /api/payments/webhook/` - webhook (заглушка)

2. **Веб-интерфейс**
   - `/payments/my/` - история платежей с фильтрацией
   - `/payments/success/{id}/` - страница успешной оплаты
   - `/payments/failed/{id}/` - страница неудачной оплаты
   - Интеграция с формой покупки абонемента

3. **База данных**
   - Модель `Payment` с полями:
     - client, membership, amount
     - status (PENDING, COMPLETED, FAILED, REFUNDED)
     - payment_method (CARD, CASH, YOOKASSA)
     - transaction_id, payment_url
     - created_at, completed_at

4. **Логика**
   - Автоматический расчет цены со скидками (Strategy Pattern)
   - Создание неактивного абонемента при создании платежа
   - Заготовка для активации абонемента после оплаты

### ⏳ Не реализовано (на финал спринта)

- Интеграция с YooKassa API
- Webhook обработчик для подтверждения оплаты
- Celery задача для таймаута платежей
- Генерация чеков/квитанций

---

## 🚀 Как запустить проект

### Вариант 1: Docker (Рекомендуется)

```bash
# 1. Перейти в корневую папку проекта
cd "C:\Users\Admin\Documents\3 курс\Семестр 1\ПИ\Курсовой_проект"

# 2. Запустить все сервисы через Docker Compose
docker-compose up -d

# 3. Дождаться запуска (1-2 минуты)
# Проверить статус:
docker-compose ps

# 4. Применить миграции (если еще не применены)
docker-compose exec web python manage.py migrate

# 5. Создать суперпользователя (если нет)
docker-compose exec web python manage.py createsuperuser

# 6. Создать тестовые данные (опционально)
docker-compose exec web python manage.py create_test_client

# 7. Открыть в браузере
# - Веб-интерфейс: http://localhost:8000
# - Админ-панель: http://localhost:8000/admin
```

### Вариант 2: Локальный запуск (без Docker)

```bash
# 1. Перейти в папку backend
cd "C:\Users\Admin\Documents\3 курс\Семестр 1\ПИ\Курсовой_проект\backend"

# 2. Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 3. Установить зависимости (если не установлены)
pip install -r requirements.txt

# 4. Запустить PostgreSQL и Redis
# (должны быть установлены и запущены отдельно)

# 5. Применить миграции
python manage.py makemigrations
python manage.py migrate

# 6. Создать суперпользователя
python manage.py createsuperuser

# 7. Запустить сервер разработки
python manage.py runserver

# 8. В отдельном терминале запустить Celery worker
celery -A config worker -l info

# 9. В третьем терминале запустить Celery beat
celery -A config beat -l info

# 10. Открыть в браузере
# - Веб-интерфейс: http://127.0.0.1:8000
# - Админ-панель: http://127.0.0.1:8000/admin
```

---

## 🧪 Как тестировать функциональность платежей

### Подготовка тестовых данных

#### Через админ-панель (http://localhost:8000/admin)

1. **Создать типы абонементов** (если нет):
   - Перейти в: `Memberships > Membership types > Add`
   - Примеры:
     - Месячный безлимит: 3000 руб, 30 дней, безлимит посещений
     - 8 занятий: 2000 руб, 30 дней, 8 посещений
     - Годовой: 30000 руб, 365 дней, безлимит

2. **Создать клиента** (если нет):
   - Перейти в: `Accounts > Profiles > Add`
   - Заполнить поля, установить role=CLIENT
   - Создать связанный Client объект
   - Опционально: установить is_student=True для скидки

#### Через management команду

```bash
# Создать тестового клиента с данными
docker-compose exec web python manage.py create_test_client

# Или локально:
python manage.py create_test_client
```

---

## 📝 Сценарии тестирования

### Сценарий 1: Покупка абонемента (без реальной оплаты)

**Цель:** Проверить создание платежа и неактивного абонемента

1. **Войти как клиент**
   - Перейти на http://localhost:8000/login/
   - Войти под тестовым клиентом

2. **Выбрать абонемент**
   - Перейти в "Абонементы" → http://localhost:8000/memberships/catalog/
   - Выбрать тип абонемента, нажать "Купить"

3. **Оформить покупку**
   - На странице покупки увидите:
     - Название абонемента
     - Базовую цену
     - Скидку (если есть)
     - Итоговую цену
   - Нажать "Купить абонемент"

4. **Результат:**
   - Перенаправление на `/payments/my/`
   - Новый платеж в статусе "Ожидает оплаты"
   - Абонемент создан, но НЕ активен (status=SUSPENDED)

**Что проверить:**
- ✅ Платеж создался с правильной суммой
- ✅ Скидка применилась корректно
- ✅ Абонемент в статусе SUSPENDED
- ✅ В "Мои абонементы" новый абонемент НЕ показывается (он неактивен)

---

### Сценарий 2: Просмотр истории платежей

**Цель:** Проверить отображение платежей

1. **Перейти в историю платежей**
   - Меню пользователя → "История платежей"
   - URL: http://localhost:8000/payments/my/

2. **Что должно отображаться:**
   - Вкладки фильтрации: Все / Ожидают / Оплачены / Неудачные
   - Список платежей с информацией:
     - ID платежа
     - Сумма
     - Абонемент
     - Статус
     - Дата создания

3. **Для ожидающих платежей:**
   - Кнопка "Оплатить" (пока не работает, т.к. нет YooKassa)
   - Или сообщение "Ожидание формирования платежа"

**Что проверить:**
- ✅ Платежи отображаются корректно
- ✅ Фильтрация по статусу работает
- ✅ Информация о платеже полная

---

### Сценарий 3: Ручная активация платежа (имитация успешной оплаты)

**Цель:** Проверить активацию абонемента после оплаты

**Через админ-панель:**

1. **Найти платеж**
   - Перейти в админку: http://localhost:8000/admin/
   - `Payments > Payments`
   - Найти нужный платеж (status=PENDING)

2. **Изменить статус**
   - Открыть платеж на редактирование
   - Изменить:
     - Status: `COMPLETED`
     - Completed at: текущая дата/время
   - Сохранить

3. **Проверить результат**
   - Перейти в `Memberships > Memberships`
   - Найти связанный абонемент
   - Проверить: status должен быть `ACTIVE`

4. **Проверить на сайте**
   - Войти как клиент
   - Перейти в "Мои абонементы"
   - Абонемент теперь отображается как активный

**Что проверить:**
- ✅ Абонемент активировался автоматически
- ✅ В "Мои абонементы" абонемент показывается
- ✅ В "История платежей" статус = "Оплачено"

---

### Сценарий 4: Страница успешной оплаты

**Цель:** Проверить страницу успеха

1. **Создать успешный платеж** (через админку, см. Сценарий 3)

2. **Перейти на страницу успеха**
   - URL: http://localhost:8000/payments/success/{payment_id}/
   - Заменить {payment_id} на ID реального платежа

3. **Что должно отображаться:**
   - ✅ Зеленая карточка с галочкой
   - ✅ Сообщение "Оплата прошла успешно!"
   - ✅ Детали платежа (сумма, дата, способ)
   - ✅ Информация об абонементе
   - ✅ Кнопки: "Записаться на занятия" и "Мои абонементы"

---

### Сценарий 5: Страница неудачной оплаты

**Цель:** Проверить страницу ошибки

1. **Создать неудачный платеж**
   - В админке изменить статус платежа на `FAILED`

2. **Перейти на страницу неудачи**
   - URL: http://localhost:8000/payments/failed/{payment_id}/

3. **Что должно отображаться:**
   - ✅ Красная карточка с крестиком
   - ✅ Сообщение "Оплата не прошла"
   - ✅ Детали платежа
   - ✅ Инструкции что делать дальше
   - ✅ Кнопка "Попробовать снова"

---

### Сценарий 6: Тестирование скидок

**Цель:** Проверить Strategy Pattern для расчета скидок

**Тест 1: Скидка для студентов (20%)**

1. Создать клиента с `is_student=True`
2. Войти под этим клиентом
3. Перейти в каталог абонементов
4. Выбрать любой абонемент
5. **Проверить:** итоговая цена = базовая цена × 0.8

**Тест 2: Без скидки**

1. Создать обычного клиента (is_student=False)
2. Купить абонемент
3. **Проверить:** итоговая цена = базовая цена

**Тест 3: Проверка через API**

```bash
# Получить расчет цены с учетом скидки
curl -X POST http://localhost:8000/api/memberships/calculate-price/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "membership_type_id": 1,
    "client_id": 1
  }'
```

**Что проверить:**
- ✅ Скидка студентам работает (20%)
- ✅ Расчет цены правильный
- ✅ Информация о скидке сохраняется в notes платежа

---

## 🔧 API тестирование через curl/Postman

### Получить список своих платежей

```bash
# 1. Получить токен (сначала войти)
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testclient",
    "password": "password123"
  }'

# 2. Получить платежи
curl -X GET http://localhost:8000/api/payments/my/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

### Создать новый платеж

```bash
curl -X POST http://localhost:8000/api/payments/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "membership_type_id": 1,
    "payment_method": "YOOKASSA"
  }'
```

### Проверить статус платежа

```bash
curl -X GET http://localhost:8000/api/payments/1/status_check/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

---

## 🐛 Известные ограничения (будут исправлены)

1. **YooKassa не интегрирован**
   - Платежи создаются, но нельзя реально оплатить
   - payment_url пустой
   - Webhook не обрабатывается

2. **Ручная активация**
   - Нужно вручную менять статус в админке
   - Автоактивация работает только при изменении через код

3. **Нет таймаута платежей**
   - PENDING платежи висят вечно
   - Celery задача для таймаута не создана

4. **Нет чеков**
   - Квитанция не генерируется
   - Email отправка пока не настроена

---

## 📊 Проверка через админ-панель

### Просмотр данных

1. **Платежи:** http://localhost:8000/admin/payments/payment/
   - Можно видеть все платежи
   - Фильтры по статусу, клиенту, дате
   - Редактирование статуса

2. **Абонементы:** http://localhost:8000/admin/memberships/membership/
   - Связь с платежами
   - Статус абонемента (ACTIVE/SUSPENDED)

3. **Клиенты:** http://localhost:8000/admin/accounts/client/
   - Все клиенты с их платежами

---

## 📈 Что работает прямо сейчас

✅ **Backend:**
- Создание платежей через API
- Сохранение в БД
- Связь Payment ↔ Membership
- Расчет скидок (Strategy Pattern)
- Создание неактивного абонемента

✅ **Frontend:**
- Форма покупки абонемента
- История платежей с фильтрацией
- Страницы успеха/неудачи
- Интеграция в навигацию

✅ **Логика:**
- Автоматический расчет цены со скидками
- Создание абонемента в статусе SUSPENDED
- Изменение статуса абонемента при оплате (через код)

---

## 🔜 Следующие шаги (конец спринта 5)

1. **Интеграция YooKassa API**
   - Установить yookassa SDK
   - Создавать реальные платежи
   - Получать payment_url
   - Редиректить клиента на оплату

2. **Webhook обработчик**
   - Проверка подписи YooKassa
   - Обновление статуса платежа
   - Активация абонемента
   - Отправка уведомлений

3. **Celery задачи**
   - Таймаут платежей (30 минут)
   - Email уведомления
   - Генерация чеков

4. **Observer Pattern**
   - Отправка email при успешной оплате
   - SMS уведомления (опционально)
   - Логирование событий

---

## 🆘 Troubleshooting

### Проблема: Платеж создается, но абонемент не виден

**Решение:** Абонемент создается в статусе SUSPENDED. Он станет видимым только после "оплаты" (изменения статуса на COMPLETED).

### Проблема: Ошибка при создании платежа

**Проверить:**
1. Клиент существует и связан с пользователем
2. Тип абонемента активен (is_active=True)
3. Пользователь авторизован

### Проблема: Скидка не применяется

**Проверить:**
1. У клиента установлен is_student=True
2. Strategy pattern работает (проверить в коде)
3. Цена рассчитывается в PaymentCreateSerializer

### Проблема: Docker контейнеры не запускаются

```bash
# Остановить все
docker-compose down

# Пересобрать
docker-compose build

# Запустить заново
docker-compose up -d

# Посмотреть логи
docker-compose logs -f web
```

---

## 📞 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи: `docker-compose logs web`
2. Убедитесь, что все миграции применены
3. Проверьте, что Redis и PostgreSQL работают
4. Проверьте настройки в `backend/config/settings/dev.py`

---

**Дата:** 2025-01-10
**Версия:** Sprint 5 (Payments) - Базовая функциональность
**Статус:** 70% готово (без YooKassa интеграции)
