# Интеграция с ЮKassa

Полная документация по интеграции платёжной системы ЮKassa в проект АС УСК.

## 📋 Содержание

1. [Настройка](#настройка)
2. [Архитектура](#архитектура)
3. [API Endpoints](#api-endpoints)
4. [Процесс оплаты](#процесс-оплаты)
5. [Тестирование](#тестирование)
6. [Troubleshooting](#troubleshooting)

---

## 🔧 Настройка

### 1. Регистрация в ЮKassa

1. Зарегистрируйтесь на https://yookassa.ru/
2. Получите тестовые данные:
   - **Shop ID** (идентификатор магазина)
   - **Secret Key** (секретный ключ)

### 2. Настройка переменных окружения

Добавьте в файл `.env`:

```bash
# Payment (ЮKassa)
YOOKASSA_SHOP_ID=your-shop-id-here
YOOKASSA_SECRET_KEY=your-secret-key-here
```

**Важно:** Для тестирования используйте **тестовые данные** из личного кабинета ЮKassa.

### 3. Настройка webhook URL

В личном кабинете ЮKassa укажите webhook URL:

```
https://your-domain.com/api/payments/webhook/
```

Для локальной разработки используйте ngrok:
```bash
ngrok http 8000
# Используйте полученный URL: https://xyz.ngrok.io/api/payments/webhook/
```

---

## 🏗️ Архитектура

### Компоненты системы

```
┌─────────────────┐
│   Frontend      │
│   (Client)      │
└────────┬────────┘
         │
         │ POST /api/payments/
         ▼
┌─────────────────┐
│  PaymentViewSet │
│  (views.py)     │
└────────┬────────┘
         │
         │ create()
         ▼
┌──────────────────────┐
│ PaymentCreateSerializer│
│  (serializers.py)     │
└────────┬───────────────┘
         │
         │ yookassa.create_payment()
         ▼
┌─────────────────┐         ┌─────────────┐
│ YooKassaService │◄────────│  ЮKassa API │
│ (yookassa_service.py)│    └─────────────┘
└────────┬────────┘
         │
         │ payment_url
         ▼
┌─────────────────┐
│   Payment Model │
│   (models.py)   │
└─────────────────┘
```

### Файлы реализации

- **`apps/payments/yookassa_service.py`** - Сервис для работы с ЮKassa API
- **`apps/payments/serializers.py`** - Создание платежа и интеграция
- **`apps/payments/views.py`** - ViewSet с endpoints и webhook
- **`apps/payments/models.py`** - Модель Payment
- **`test_yookassa_integration.py`** - Тестовый скрипт

---

## 🔌 API Endpoints

### 1. Создание платежа

**POST** `/api/payments/`

Создаёт платёж и возвращает URL для оплаты.

**Request:**
```json
{
  "membership_type_id": 1,
  "payment_method": "YOOKASSA"
}
```

**Response:**
```json
{
  "id": 42,
  "client": 5,
  "client_name": "Иван Иванов",
  "amount": "4500.00",
  "status": "PENDING",
  "payment_url": "https://yoomoney.ru/checkout/payments/v2/contract?orderId=...",
  "transaction_id": "2d96e1b2-000f-5000-8000-18db351245c7",
  "created_at": "2025-11-17T12:00:00Z"
}
```

**Процесс:**
1. Создаётся абонемент со статусом `SUSPENDED`
2. Рассчитывается цена со скидками (Strategy pattern)
3. Создаётся платёж в ЮKassa
4. Возвращается `payment_url` для редиректа клиента

### 2. Получение списка платежей

**GET** `/api/payments/my/`

Возвращает платежи текущего клиента.

**Query params:**
- `status` - фильтр по статусу (PENDING, COMPLETED, FAILED)

**Response:**
```json
[
  {
    "id": 42,
    "amount": "4500.00",
    "status": "COMPLETED",
    "status_display": "Оплачено",
    "created_at": "2025-11-17T12:00:00Z",
    "completed_at": "2025-11-17T12:05:23Z"
  }
]
```

### 3. Проверка статуса платежа

**GET** `/api/payments/{id}/status_check/`

Проверяет актуальный статус в ЮKassa и обновляет в БД.

**Response:**
```json
{
  "id": 42,
  "status": "COMPLETED",
  "payment_url": "...",
  "transaction_id": "...",
  "completed_at": "2025-11-17T12:05:23Z"
}
```

### 4. Webhook (для ЮKassa)

**POST** `/api/payments/webhook/`

**Важно:** Это endpoint для ЮKassa, не для фронтенда!

**Автоматически:**
- Обрабатывает уведомления от ЮKassa
- Обновляет статус платежа
- Активирует абонемент при успешной оплате

**События:**
- `payment.succeeded` - оплата успешна
- `payment.canceled` - оплата отменена

---

## 💳 Процесс оплаты

### Шаг 1: Клиент выбирает абонемент

Фронтенд отправляет запрос:

```javascript
POST /api/payments/
{
  "membership_type_id": 1,
  "payment_method": "YOOKASSA"
}
```

### Шаг 2: Backend создаёт платёж

1. Рассчитывает цену со скидками
2. Создаёт абонемент (неактивный)
3. Создаёт запись Payment в БД
4. Отправляет запрос в ЮKassa API
5. Получает `payment_url`

### Шаг 3: Клиент переходит на страницу оплаты

```javascript
// Редирект на payment_url
window.location.href = response.data.payment_url;
```

### Шаг 4: Клиент оплачивает

- Вводит данные карты на странице ЮKassa
- ЮKassa обрабатывает платёж

### Шаг 5: ЮKassa отправляет webhook

```
POST /api/payments/webhook/
{
  "event": "payment.succeeded",
  "object": {
    "id": "2d96e1b2-000f-5000-8000-18db351245c7",
    "status": "succeeded",
    "paid": true,
    "metadata": {
      "payment_id": "42"
    }
  }
}
```

### Шаг 6: Backend обрабатывает webhook

1. Находит платёж по `payment_id` из metadata
2. Обновляет статус на `COMPLETED`
3. Активирует абонемент (`ACTIVE`)
4. (Опционально) Отправляет уведомление клиенту

### Шаг 7: Клиент возвращается на сайт

ЮKassa редиректит на:
```
http://your-domain.com/payments/success/?payment_id=42
```

---

## 🧪 Тестирование

### Запуск тестового скрипта

```bash
cd backend
python test_yookassa_integration.py
```

**Скрипт проверяет:**
1. ✅ Инициализацию YooKassaService
2. ✅ Создание платежа в ЮKassa
3. ✅ Создание платежа через API
4. ✅ Обработку webhook
5. ✅ Активацию абонемента после оплаты

### Тестовые карты ЮKassa

Для тестирования используйте тестовые карты:

**Успешная оплата:**
- Номер: `5555 5555 5555 4444`
- Срок: любой будущий месяц/год
- CVC: любой 3-значный код

**Отмена платежа:**
- Номер: `5555 5555 5555 5599`

Полный список: https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing

### Проверка webhook локально

1. Установите ngrok:
```bash
brew install ngrok  # macOS
# или скачайте с https://ngrok.com/
```

2. Запустите туннель:
```bash
ngrok http 8000
```

3. Скопируйте URL (например, `https://abc123.ngrok.io`)

4. В личном кабинете ЮKassa укажите:
```
https://abc123.ngrok.io/api/payments/webhook/
```

5. Создайте тестовый платёж и оплатите тестовой картой

6. Проверьте логи:
```bash
docker-compose logs -f backend
```

---

## 🔍 Troubleshooting

### Ошибка: "Ошибка создания платежа: Unauthorized"

**Причина:** Неверные `YOOKASSA_SHOP_ID` или `YOOKASSA_SECRET_KEY`

**Решение:**
1. Проверьте `.env` файл
2. Используйте тестовые данные из личного кабинета ЮKassa
3. Перезапустите Docker: `docker-compose restart backend`

### Webhook не приходит

**Причина:** ЮKassa не может достучаться до вашего сервера

**Решение:**
1. Используйте ngrok для локальной разработки
2. Проверьте URL в личном кабинете ЮKassa
3. Убедитесь, что endpoint `/api/payments/webhook/` доступен без авторизации

### Платёж создан, но статус не обновляется

**Причина:** Webhook не обработан или произошла ошибка

**Решение:**
1. Проверьте логи: `docker-compose logs backend`
2. Вручную проверьте статус: `GET /api/payments/{id}/status_check/`
3. Проверьте, что в `metadata` передаётся правильный `payment_id`

### Абонемент не активируется после оплаты

**Причина:** Ошибка в обработке webhook

**Решение:**
1. Проверьте статус платежа в БД:
```sql
SELECT id, status, transaction_id FROM payments_payment WHERE id=42;
```

2. Проверьте статус абонемента:
```sql
SELECT id, status FROM memberships_membership WHERE id=...;
```

3. Вручную активируйте через Django admin или shell

### Тестовые платежи не проходят

**Причина:** Используются боевые данные вместо тестовых

**Решение:**
1. В личном кабинете ЮKassa переключитесь на **Тестовый режим**
2. Используйте тестовые `SHOP_ID` и `SECRET_KEY`
3. Используйте тестовые карты (см. выше)

---

## 📚 Дополнительные ресурсы

- [Официальная документация ЮKassa](https://yookassa.ru/developers/api)
- [Python SDK для ЮKassa](https://github.com/yoomoney/yookassa-sdk-python)
- [Тестирование платежей](https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing)
- [Webhook уведомления](https://yookassa.ru/developers/using-api/webhooks)

---

## ✅ Checklist для Production

- [ ] Получены **боевые** Shop ID и Secret Key
- [ ] Настроен **HTTPS** для webhook URL
- [ ] Webhook URL добавлен в личном кабинете ЮKassa
- [ ] Проверена обработка всех статусов платежей
- [ ] Настроено логирование ошибок
- [ ] Добавлены уведомления клиентам (email/SMS)
- [ ] Настроена обработка возвратов (refunds)
- [ ] Проведено нагрузочное тестирование

---

**Автор:** Команда разработки АС УСК
**Дата:** 2025-11-17
**Версия:** 1.0
